WITH deduped AS (
  SELECT *,
         ROW_NUMBER() OVER (PARTITION BY MDM_ID ORDER BY review_category DESC) AS rn
  FROM TEMP1
)
SELECT Category, review_category, COUNT(*) AS Unique_MDMs
FROM deduped
WHERE rn = 1
GROUP BY Category, review_category